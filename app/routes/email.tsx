import { Heading } from "@radix-ui/themes";
import Layout from "~/components/Layout";

const EmailSettings = () => {
	return (
		<Layout>
			<Heading as="h2">Email settings</Heading>
		</Layout>
	);
};

export default EmailSettings;
